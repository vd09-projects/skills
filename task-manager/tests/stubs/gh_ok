#!/usr/bin/env bash
# Stub: gh present and `gh repo view` succeeds.
case "${1:-}" in
  repo)
    if [[ "${2:-}" == "view" ]]; then
      echo '{"nameWithOwner":"acme/widget"}'
      exit 0
    fi
    ;;
esac
exit 0
